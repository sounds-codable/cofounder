import { DetailProfileForm } from '@/components/detail-profile-form';

export default function DetailOnboardingPage() {
  return (
    <div className="site-shell narrow-shell page-section page-stack">
      <DetailProfileForm />
    </div>
  );
}
