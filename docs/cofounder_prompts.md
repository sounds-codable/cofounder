我想把这个项目重构，目前前端使用taro，我想替换成 nextjs 最新版本 v16。 后端保持使用nestjs不变化。

此项目要实现的事：
1. 分成 项目方（也称为 行业专家） 和 程序员 两个角色。

2. 两种角色都可以在本平台录入信息。

3. 项目方录入信息：
a. 基础信息：录入项目情况，只需要录入 2项最基本的信息：项目描述，所在城市。
b. 详细信息：包含个人简介，包括 教育背景（学校、专业），工作背景（哪些公司，做过哪些事）；项目详请（关于项目的更多描述），联系方式（电话、微信、QQ、邮箱 等选填）

4. 程序员的录入信息：
a. 基础信息：录入技能基本情况，只需要录入3项最基本的信息：会哪些技能及做过哪些项目，想做什么方向/类型的项目（选填），所在城市。
b. 详细信息：包含个人简介，包括 教育背景（学校、专业），工作背景（哪些公司，做过哪些事），做过的产品/项目介绍，联系方式（电话、微信、QQ、邮箱 等选填）

5. 信息录入的流程：
a. 未登录用户，可以查看 项目方基础信息、程序员基础信息；
b. 收藏、点赞、了解详情（即发送请求给发布者，希望查看详请）这些功能，需要用户登录；
c. 用户登录后，可以 收藏、点赞，直接点一下即可完成；但点 了解 的逻辑比较复杂，如下。
d. 了解详请 功能：
- 了解详请的按钮 要醒目，比 收藏、点赞 要明显很多，可以放在不同的位置
- 分成 发布者 和 意向者（想联系发布者的用户），发布者 和 意向者 可能是项目方，也可能是程序员。
- 发布者不能给自己发的项目点 了解详请
- 意向者给发布者发的项目点 了解详请 时，要求 意向者 先填写其 详细信息（见上面 项目方和程序员 的 详细信息 部分） （如果已经填写过了，就直接可以发送 请求给到发布者），
- 发布者 收到 请求者 发送的 了解详请 请求者，可以直接看到 请求者的基础信息，并且，可以看到请求者填写的 详细信息（但是内容打码了，让发布者知道 请求者 写了很多字，但是看不到具体写了啥），然后，他可以点：
-- 查看 对方详细信息 按钮 （系统先检查 发布者是否已经写好 详细信息，如果写好了，才让看，但是意向者的 详细信息 中的 联系方式 部分不能看；没写好，让发布者先写 详细信息）
-- 同意/拒绝 请求的按钮 （在点这个按钮之前，须先点过 查看对方详细信息 按钮）
--- 同意 按钮，其 详细信息 意向者能够看到，并且，在点再出现一个弹框，让写上 自己的联系方式 （从其详细信息里填写的联系方式中显示出来，让勾选一个，如果没有，他可以在此form中直接添加到详细信息里联系信息里，然后再勾选这个），告知发布者，如果同意让对方看详细信息，联系方式也将发送给意向者，是希望和意向者进一步聊（比如通过微信）
--- 拒绝按钮，如果点这个，要写一个理由，说明下为啥决绝 给意向者 看其详请。这样意向者就看不到其详请。

- 意向者在收到 发布者 发来的 同意/拒绝 信息后：
--同意：意向者能够看到 发布者发来的详细信息，但是，发布者写的联系信息不能看到，需要再 点   交换联系信息 按钮，需要同时将自己的信息发送给 发布者 ，才能看到。（注：点这个 交换联系信息 按钮，在意向者页面再出现一个弹框，让写上 意向者自己的联系方式 （从其详细信息里填写的联系方式中显示出来，让勾选一个，如果没有，他可以在此form中直接添加到详细信息里联系信息里，然后再勾选这个）
 

6. 以上 分步骤 让填写信息的方式，背后的设计是希望用户一开始只提交最少的信息，只有当他有进一步需求时（如查看其它用户的信息或项目信息），才让填写更多的信息。并且，发布者 和 意向者 能看到彼此信息是需要有一个双向确认/同意的过程的。
在页面中，详细信息 只需要填写一次 要给到提示，并且，要给出解释，填写详细信息是为了能够让对方更好地了解自己，减少沟通/信息成本，但这些信息只有在得到授权后才会给对方看到；并且，联系信息会特别强调，只有授权后才会被对方看到，并且，设计上，要特别 标示出来，点了每个按钮之后，对方将会看到什么，不会看到什么。

现在需要做的事：
1. 将 docs 里的 @Claude-PRD.md 和 @需求文档-分步骤完善信息与联系解锁.md 删掉，改为 写一份新的需求文档，内容根据我上面说的这些来写，然后用这2份 要删掉的文档中的内容作补充，如果有冲突部分，以我上面说的为准。这2份老的文档的内容已经过时了。
2. 将 READMD.md 更新成与上面说的一致。
3. 先不用进行实际的开发工作，等文档ok之后，我再告诉你开始实际的开发。

--
1. client及server 移到目录 archive 里，再写一个文档，告诉如何访问这些旧的页面（命令，url）
2. 从0开始做此项目，前端用nextjs 最新版本 （v16），后端用 nestjs最新版本（v11)；
3. 前端要做 pc 和 手机上页面，要现代、美观、简洁；不用考虑app和微信小程序，现在不做，下一版再做（预留后面升级的可能性，比如在目录起名字时要有考虑）
4. db 仍然使用 postgresql，db 本地要用个新的 数据库，名为 cofounder_new，db服务器端清空原有db中的内容，原有db名称不变。要写一个docs，包含如何在本地mac上创建此新的db。
5. 原先的 @scripts @deployment.md 部署方式不变，继续使用。

--


页尾：@2026 叩饭（Cofounder）｜让真实项目与真实能力先被看见，再让真正匹配的合作自然发生。


要分成3行显示，浙ICP XXX 浙公网安备 XXX号 这2个各一行

另外，首页的menu 现在为 项目方  程序员 
改为：
- 项目库  （注：这个显示所有项目 页）
- 程序员（注：这个显示所有程序员 页）
- 公益 （注：做一个新页面，说明是 纯公益，所有环节都无收费设置，希望营造一个我为人人、人人为我的 乌托邦，也是与 被AI替代 的自救。招募志愿者/共建者，比如律师、投资人、账务人士等等创业路上能给帮助的人，欢迎留言 或 写信给 cofounder@cofounder.icu
（注：要做一下这个功能，使能够提交留言，让留联系方式和一个多文本的框）

如果你的项目通过这个社区的帮助获得了成功，也欢迎赞助我们，让我们能够请人来更好地运营这个社群。

- 缘起 （注：做一个页面，介绍一下我们为什么要做这个公益的站，大致是说2025年开始AI快速在替代互联网行业的工作，预计会迅速蔓延到更多行业。与此同时，
70、80、90 后 们在各行各业积累深刻的行业经验，知道有人付费的真需求在哪里，这行经验如果转换成一个个应用/软件/平台，可以创业，提升个人价值，也能提高社会效率。
AI coding 虽然越来越容易，替代了大量的程序员，但对于通常上了年纪的行业专家而言，去学vibe coding 困难又不合算；而程序员借助AI，可以快速做出许多原本无法实现的产品，但是对细分行业的理解和资源却非常难获取。
因此，将双方匹配，是最好的选择。
我们先抛砖引玉，通过这个网站来创建一个社群，这个社群不属于我们，而是属于大家。

注：大致是以上文字，2个新建的页面，都帮我完善一下，文风要平易近人，通俗易懂，接地气，不要太高大上，要诚恳，谦虚，就像好朋友之间在聊天那样。
--
http://localhost:3000/
当前的首页，后台的各页面，是按照 @产品需求文档-角色双向授权与Nextjs重构.md 做好了，但是，有以下问题：

1. 现在各页面把所有的产品逻辑都写出来了！这是不对的，很多产品逻辑是用来告诉 llm 做 vibe coding用的，是开发思路，并不是要给用户看的；另外，就算是给用户看的信息，也不是直接全都直接显示在页面中，太乱了，太多了，应该通过设置 提示icon 等方式 在用户点击后再显示；而且，每个信息都应该在到了对应的页面/步骤时才显示，用户没用到时，就不要显示。把所有页面都仔细检查一下。

2. 当前的首页，将文字改成 @client 老项目中的首页的文字，基本上和老的首页一样，只是把老首页中的 让填写邮箱 (waitlist）改成 我是项目方  我是程序员  这样的引到入口

3. 其它页面，都不要参考老项目，完全按照最合适的方式来梳理。

4. 做之前，你再仔细看一下 @产品需求文档-角色双向授权与Nextjs重构.md ，领会精神和要求。

--
如图1，有一篇blog，点击 继续阅读 之后，显示如图2。

但是，在本地测试是ok的（通过 ./scripts/dev.sh启动），服务器上不行。

服务器上的配置：@nginx.cofounder.icu.conf @supervisor.cofounder-backend.conf @supervisor.cofounder-frontend.conf 
--
此项目通过 @deploy.sh 部署到服务器上ubuntu24.04，配置：@nginx.cofounder.icu.conf ，@supervisor.cofounder-backend.conf ，@supervisor.cofounder-frontend.conf 

现在访问 https://cofounder.icu/projects
会先闪现2个卡片（编号：p-00001 和 编号：p-00002 ），这2个卡片已经在db中删除了，但会闪现一下（1s不到），然后马上就不见了，显示出如图这样正确的页面（db中现在只有 p-00003 这一个项目）

找出原因，修复！
--

如图1，输入项目 或 登记程序员 信息时，输入时是有段落的，但是显示出来后，就没有格式和段落了（如图2）。

修复！

另外，如果有成熟的、主流的、安全的、适合手机和pc端输入的所见即所得的简单的编辑器，也可以引用，但要性能高一些，格式支持不需要多，只需要如 大小，加粗等基本的就行。如果能支持markdown就更好了。
但一定要注意，用户输入的东西可能是不安全的，要做足安全防范，避免黑客入侵。
--
如图，部署到服务器上 （ubuntu 24.04）访问  https://cofounder.icu/p00003/%E5%8C%BB%E7%96%97%E5%A4%A7%E5%81%A5%E5%BA%B7%E8%A1%8C%E4%B8%9A%E9%A1%B9%E7%9B%AE 应该显示项目详请页，但现在是显示首页了！并且，没有任何报错信息，chrome里看不到。服务器上使用的 配置：@nginx.cofounder.icu.conf @supervisor.cofounder-backend.conf 

然而，在本地（mac)上调试是好的，
--
此项目部署到服务器上（ubuntu24.04）后，登录页面，输入验证码后，显示如图，但是再点击一下刷新，页面就又能加载出来啦。修复。

@nginx.cofounder.icu.conf @supervisor.cofounder-backend.conf 这2个是服务器上的配置
--
如图，输入 验证码之后，提示 请求过于频繁，请在 60 秒后再试


但实际上是第一次提交，没有重试过。修复！并且，限制频率放宽一些
--
当前 @nginx.cofounder.icu.conf 配置会限制 发送验证码 （login页面），取消这个频率限制，或者放得非常宽松。现在用户第一次点发送验证码就会提示频率过快，导致完全不可用。
--
如图，在登录前、登录后的所有页面，这个头像边上，加一个头像，表示 社群，点开后，显示一个弹框 （样式好看点，与本项目其它弹框相同或相似样式），显示：

叩饭（Cofounder）是一个公益社群，正在招运营的小伙伴，负责建一个群，组织线下活动，在社媒上宣传叩饭。无报酬，志愿者。
有兴趣者，联系 x@cofounder.icu 附上简要介绍。
（注：帮我优化文案，亲切，诚恳，情怀，接地气不要高大上，让用户愿意来联系）
--
如图，在本地 直接访问 http://localhost:3000/login 登录之后，会自动跳转到 http://localhost:3000/projects

但是，在服务器上，登录之后，没有自动跳转，而是停留在如图页面1。要手动点进入后台才行。sign out之后，再试一次，这次登录后如图2.
线上的配置：@nginx.cofounder.icu.conf @supervisor.cofounder-backend.conf 
--
如图，现在每次执行 ./scripts/deploy.sh 时，会自动向服务器的 db 中添加 模拟数据。

也再检查一下，会不会给其它表中也添加数据。

我希望部署时，只补齐表结构，但不要添加任何模拟数据！
--
如图，出现一个问题，直接访问链接，会403，但首页可以访问，从首页的menu等link里点过去是可以访问的。@nginx.cofounder.icu.conf @supervisor.cofounder-backend.conf 服务器上的配置是这2个，修复！
要允许直接访问link

此外，@mail.service.ts#L25-32 这个邮件的 subject 标题要用 验证码开头 方便用户看
--
我希望本站的用户登录之后，能够长时间保持登录状态，比如7天到1个月之间。
我的服务器上使用了 redis，但redis的配置是不持久的，因此其它项目是直接使用了 file 来保存session。

本项目目前是用什么方式来保持登录状态，应该改成？
我希望服务器上不要产生太多 file （登录的用户不会多，最多几百，但搜索引擎访问可能会比较多；搜索引擎可访问的，都是非登录页，也不需要session）。
--
如图，邀请码：
- 默认生成的邀请码与用户的昵称一致，如果重复了，在后面加数字（数字逻辑与 自动生成用户昵称的逻辑相同，请复用，即优先加 66,666,6666，所有数字避免4）
- 邀请码用户可以修改，也和昵称的修改逻辑一样，全站唯一，不可重复
--
分析整个项目的安全性。此项目通过 @deploy.sh 部署在 ubuntu 24.04 服务器上，使用 supervisor 控制后端。
使用的配置为：@nginx.cofounder.icu.conf @supervisor.cofounder-backend.conf
其中，nginx的配置，在服务器上我手动执行了 certbot -d 命令加了 ssl 
--
如图，程序员用户：
第1次 别的用户向他询问更多信息时，他有提示了一次信息；
第2次，此程序员向其它用户发的项目询问更多信息时，他再次提供信息时，有修改，这次提交的信息不应该影响第1次提交的，但是，现在第1次提交的信息也显示了第2次改的！应该两者是独立的。

帮我检查是后端功能/db 没处理好，还是前端显示没处理好？

并且检查整个项目，这种显示信息的部分，如果是有快照的，是不是都已经正确地显示了快照？
检查后，将检查结果列清单告诉我，放到 docs 中
--
如图1，蓝框里的条款，需要更加有法律效力，可以参考下面这段：
---
法律声明与免责约定

平台仅提供信息展示与沟通工具，不参与任何线下接触、交易决策、合同签署、资金流转、股权安排或争议处理。用户之间因沟通、合作或交易引发的任何直接或间接损失、纠纷与法律后果，概由相关用户自行承担全部法律责任，平台免除一切担保、连带及赔偿责任。

特别提示：您点击联系对方或使用沟通功能的行为，将被法律视为您已充分阅读、理解并自愿接受上述全部条款的约束。
---
重点是，让用户知道，联系对方了，就代表接受这个条款了。

2. 如图2，这2个按钮，也要放到如图3的蓝框处。只要图3的页面有一个申请是匹配成功的，就显示这2个按钮。

3. 如图4、图5中：
- 称呼 不能算其它联系方式，这项的标题应该就是 称呼
- 不要直接将称呼和联系方式下的内容直接显示出来，要先马赛克一下，然后旁边出现一个 查看按钮，点击后，出现一些条款，大致意思与风险提示里的一样的，就是说平台上所有的信息都未经人工核实，需要用户自行判断，发生的一切行为及后果与本平台无关……（帮我完善，要非常容易懂，条款少，一看就明白；同时也要能有法律效力！），再加上2个按钮，一个是 接受条款并查看联系方式  另一个是放弃查看。
- 每次点击查看，都要显示上面这个弹框 并让选择，只有接受的，才显示出来。

--
如图，对 项目 和 程序员 询问更多信息 时，提供的 图中的这些详请的信息，每次询问提交的内容都要单独存。

也就是说，下一次询问时，提交的内容可以与之前的不同（但默认显示上次填写的内容，可修改），下一次提交后，不改变之前已经提交过的内容
--
如图，点 分享按钮后  弹框被限制在灰色的那个区块里了，应该在最上面一层，不被限制

另外，『来分享吧！让更多人知道这个有趣的项目和叩饭（Cofounder），越多人看到，越能帮助更多人匹配到合适的Cofounder。』
这个文字， 对于 项目  和 程序员 应该是不一样的文字 调整
--
如图1，程序员信息详请页，左侧的nav应该显示，就像图2这样显示左侧nav

同样的，项目的详请页，也要显示左侧的nav
--
如图，登记程序员信息，一个用户只能登记一个。当他登记过之后：
- 将此用户登记的，显示在此页面中的第一个，并且，他的卡片的左上角的  程序员 显示为  自己
- 上面的 登记程序员信息 按钮变为不可点 点击后提示 您已登记过信息 （注：优化文案）
--
昵称 要全站唯一 写昵称时，要检查是否有重复的。
而新用户自动生成的昵称，如果有重复的，在后面加上数字以区分，加的数字优先用 66, 666，6666这几个，如果用完了，再用别的，但任何位置都避免出现任何数字4
--
如图，现在首页不同section之间的分隔，有的是图1这种样式，但也有的是图2这样没有做不同section之间的区分。统一改为使用图1这种样式做不同section的区分
--
首页，在 图1、图2、图3  在这3块内容之间，没有做每一屏幕之间的分隔，而图3的内容和其下面的其它内容之间，是做了不同屏之间的分隔。

首页的设计是，图1、图2、图3 这些的内容区块都分别一整屏幕显示，好看一点，但现在没有做区分，感觉有点怪。参考图3和其后面内容之间的区分形式 （大致就是背景颜色有变化，视觉上感觉是不同的部分）
--
分析本站内容，做 seo geo aeo 等等的优化。
--
如图，详请页的 url，显示完整的 标题，如：
http://localhost:3000/p-00002/%E8%B7%A8%E5%A2%83%E5%93%81%E7%89%8C%E5%87%BA%E6%B5%B7%E9%A1%B9%E7%9B%AE%E5%AF%BB%E6%89%BE%E5%85%A8%E6%A0%88%E7%A8%8B%E5%BA%8F%E5%91%98-%E5%B8%8C%E6%9C%9B

应该显示为：
http://localhost:3000/p-00002/跨境品牌出海项目寻找全栈程序员，希望一起快速验证 AI 增长工具方向

注意：需要将标题中的符号等转成 url 兼容，不要破坏url 
--
如图，提交留言之后，应该弹出一个弹框来显示：感谢！留言已收到，我们会认真阅读并回复。
这样的成功提示。如果有错误之处，也应该有更明显的提示，比如相应的filed 红色框起来之类的
--
如图，提交表单时，报错显示英文了。检查全站，用户提交form之类的交互时，报错全部要使用中文，不要用英文
--
此项目在mac上，chrome里正常访问，但在 safari 里会有些兼容问题，显示有些小问题。

帮我检查下整个项目，使能尽量兼容中国大陆的浏览器（比如360、腾讯的），以及中国大陆的手机端的浏览器（比如华为的，Oppo的，iphone的，UC）
--
如图，蓝框里的编号是如何来的？可否与 url 相似或一致？或者，与 分享按钮里生成的 https://localhost:3000/00016 这种 00016一致？并且，expert-p-44b7e6c2-mnudayp8er34
中的 expert-p- 开头，可以改为 p-
同时，程序员信息的页面 developer-p-44b7e6c2-mnudf6tm17vx 改应该改成 d- 开头，后面的编号也与类似地改成 与00016 类似这样一致，方便查看

同时，由于中国人普遍避讳 数字4，所以，生成的 5位数字，在任何时候，都要避免在任意位数出现4。
--
如图，将分享时 的链接  http://localhost:3000/00015
改成 https 开头的
--
本项目的网址：
http://localhost:3000/cards/expert-p-44b7e6c2-mnu5dlamjeyk

和 
http://localhost:3000/cards/developer-peter-0e42a790-mnq0b6hlgmsg

要改成尽量简短的网址，比如直接删掉cards，然后将 expert 用 p表示，developer 用 d 表示，然后后面的 44b7e6c2-mnu5dlamjeyk 改为更简短的 现在本站的项目很少 可能最多几百个，而且最好能让人/seo更容易理解的，现在这种代码完全看不懂意思。

这样做的原因：
1. 我希望页面的 url 对seo和人类用户是有含义的，这个角度上来说，url 直接能显示出汉字（即项目标题，但要处理不合适的字符，避免url出错）
2. 转发/分享 时的链接，我希望尽量简短，这些内容会显示在用户的社交媒体上，比如微信朋友圈、小红书笔记上。我有个疑问，用户的电脑/手机 都能正常显示出含中文的url吗？如果能显示出，能正常copy到浏览器中打开吗？如果不能，那还是简短无中文的链接更好吧？
--
如图1和2，我想在 蓝框区域的 收藏 点赞 后面加一个 转发 按钮。

我想实现：
1. 手机上访问时，可以直接转发到微信朋友圈、转发给微信好友、小红书发笔记、微博、知乎 等主流社交媒体。
2. 电脑上访问时，也能方便地转发到社交媒体。

我想到2种方案：
- 找到某个主流、成熟的库，可以一键集成这些功能，有这种吗？需要我去微信公共平台申请一些资质或api key 吗？
- 只帮助用户生成 可分享使用的 文本，弹框里显示，方便用户修改、复制 即可

你觉得哪个方案可行？
比较两种方式先，我确认后再开发
